addFilter("explicit-lib-dependency")
# There is permission file, but it only works as documentation by default,
# meaning there is no extra caps by default.
setBadness("permissions-unauthorized-file", 0)
# A C preprocessor file(version.h) generation, so this is false positive.
addFilter("macro-in-comment %{version}")
